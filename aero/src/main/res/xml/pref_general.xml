<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <ListPreference
        android:key="restart_delay"
        android:title="@string/pref_title_restart_delay"
        android:summary="@string/pref_summary_restart_delay"
        android:entries="@array/pref_restart_delay_titles"
        android:entryValues="@array/pref_restart_delay_values"
        android:defaultValue="2"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

</PreferenceScreen>
